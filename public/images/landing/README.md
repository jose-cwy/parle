# Landing hero images

Add your hero photos here:

- `hero-primary.jpg` — main hero image (larger frame, right side of collage)
- `hero-secondary.jpg` — secondary image (smaller overlapping frame)

Recommended: warm, soft mood (journal, night window, hands writing). Avoid corporate stock smiles.

Until these files exist, the site falls back to `/images/ref1.jpg`.
